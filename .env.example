# Variables para docker-compose.yml (nivel raíz del proyecto)
# Copiar a .env y ajustar según entorno.

# Base de datos
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=facturas_db
DB_PORT=5432

# Puertos expuestos en el host
API_PORT=8000
WEB_PORT=5173
